# Core

- Repo root: `/mnt/c/Users/endri/Desktop/Bachelor`.
- Main implementation lives in `Mobile Picking und Voice Assistant/`; top-level README and `Projekt-Wiki/` provide project context.
- Read `mem:mobile/core` for source map and invariants of the application module.
- Current repo may contain local/uncommitted user work; inspect `git status --short` before edits and do not revert unrelated changes.
- Generated/cached areas to ignore unless explicitly relevant: `node_modules/`, `backend/**/__pycache__/`, `.pytest_cache/`, `test-results/`, `.claude/artifacts/`, `graphify/`.