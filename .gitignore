# ── Private / local-only ────────────────────────────────────────────────────
# Claude Code config & memory — stays local, not for the repo
.claude/
# Project-level Claude instructions — local only
CLAUDE.md
# Obsidian vault — private notes, not for the repo
Notzien/
# Private/generated image collections, archived outside the workspace
Bilder/
# Root-level workflow wrappers — local convenience scripts
workflow.cmd
workflow.ps1

# ── Tool directories ─────────────────────────────────────────────────────────
.claude-flow/
.swarm/
.ruflo/
.playwright-mcp/
.vscode/

# ── OS files ─────────────────────────────────────────────────────────────────
.DS_Store
Thumbs.db

# ── Common caches ─────────────────────────────────────────────────────────────
**/__pycache__/
**/.pytest_cache/
**/.deps/
**/node_modules/

# ── Playwright artifacts ──────────────────────────────────────────────────────
**/.playwright-cli/
**/.playwright-mcp/
**/playwright-report/
**/test-results/

# ── Archive & media ───────────────────────────────────────────────────────────
*.zip
*.mp4
*.MP4
*.mov
*.MOV

# ── Generated cleanup targets ─────────────────────────────────────────────────
output/
**/output/
tmp_chrome_*
n8n-workflow-debug.png
pdf-rebuild-tool-staging/
mobile-picking-assistant-v2/
n8n-workflows-v2/
Mobile Picking und Voice Assistant/*.png
Mobile Picking und Voice Assistant/Detail
Mobile Picking und Voice Assistant/Picker
Mobile Picking und Voice Assistant/img

# ── Project Specific Ignores ─────────────────────────────────────────────────
.mcp.json
*.env
env-backup-*.env
.gemini-skills/
.gemini/
