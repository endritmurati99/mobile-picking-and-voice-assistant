# ── Private / local-only ────────────────────────────────────────────────────
# Claude Code config & memory — stays local, not for the repo
.claude/
# Project-level Claude instructions — local only
CLAUDE.md
# Obsidian vault — private notes, not for the repo
Notzien/
# Root-level workflow wrappers — local convenience scripts
workflow.cmd
workflow.ps1

# ── Tool directories ─────────────────────────────────────────────────────────
.claude-flow/
.swarm/
.ruflo/
.vscode/

# ── OS files ─────────────────────────────────────────────────────────────────
.DS_Store
Thumbs.db

# ── Common caches ─────────────────────────────────────────────────────────────
**/__pycache__/
**/.pytest_cache/
**/.deps/
**/node_modules/

# ── Playwright artifacts ──────────────────────────────────────────────────────
**/playwright-report/
**/test-results/

# ── Archive & media ───────────────────────────────────────────────────────────
*.zip
*.mp4
*.MP4
*.mov
*.MOV
