# Handoff — end of session 2026-07-29

Read this first tomorrow, then `docs/superpowers/parallel/2026-07-23-program-status.md` for the
programme state and the debt register.

**The authoritative per-lane progress lives in `.superpowers/sdd/<plan>/progress.md`.** Those are
gitignored scratch and do NOT survive a clone — they are on this machine only. After any context
loss, trust those ledgers plus `git log`, never recollection.

---

## 1. Where each lane stands

| Lane | Branch | Head | Done | Next |
|------|--------|------|------|------|
| R1 Backend | `remediation/r1-backend` | `879037f` | Tasks 1, 2 | Task 3 — **fix round 2 is under re-review, result not yet seen** |
| R2 Odoo | `remediation/r2-odoo` (main tree) | `a1a0f8f` | Tasks 1, 2, 3 | Task 4 (dispatched, then stopped before it read anything — restart clean) |
| R3 n8n | `remediation/r3-n8n` | `6103483` | Tasks 1, 2, 3 | Task 4 — **fix round 1 complete, needs its scoped re-review** |
| R4 DB | not started | — | — | blocked on R2 finishing; owns the Docker probes |

All three worktrees are clean. The main tree carries only documentation changes, committed at the
end of this session.

### Immediately outstanding

1. **R1 Task 3 — START HERE. The fix-round-2 verdict arrived after the session closed and it is a
   NEW CRITICAL. This is the second consecutive round of this task where the fix opened something
   else.** No fix round has been dispatched for it; that is tomorrow's first action.

   What IS closed: the delimiter bypass (`q}BI`, `q{BI` rejected raw and compressed), verified by
   the reviewer's own independent 0x00–0xFF sweep of both positions — the classes are exactly
   Table 1 ∪ {VT, documented} ∪ the documented delimiter subsets, and the constant-substitution
   construction escapes correctly, so the pattern cannot drift from the spec without editing the
   constants. The three originally reported I3 attacks are also fixed, and a legitimate raster is
   still exempt (no over-rejection).

   **The new Critical:** `_exempt_raster_idnums` pass 2 skips a node wholesale when
   `id(node) in xobject_dictionaries`. So **any dictionary doing double duty as the
   `/Resources`→`/XObject` dict and as something else has all of its outgoing references excused**,
   and a stream that really is executed re-earns the exemption just by claiming `/Type /XObject` +
   `/Subtype /Image` — precisely the failure the inversion was meant to end. Three working PoCs,
   all ACCEPTED by `validate_pdf` at `879037f`:
   - **A1** — a Type 3 font's `/CharProcs` *is* the page's `/XObject` dict; the glyph stream carries
     the image keys. 1222 B, fully conforming Type 3 font.
   - **A2** — the page dict itself is hung as `/Resources /XObject`, so its own `/Contents` counts
     as no reference and the page content stream is exempted. 665 B.
   - **A3** — a `/Pattern` dict *is* the `/XObject` dict; the tiling pattern stream carries the
     image keys. 960 B.
   A1 and A3 are conforming PDFs. All three render the inline image in any conforming interpreter.
   PoC scripts are in the session scratchpad (`rr2_attack.py`, PoC A1 at `rr2_a1.pdf`).

   **Fix direction:** the exemption must also prove the *slot* is unaliased — record how each dict
   is reached (by `idnum`, with the parent key) and drop the exemption unless the `/XObject` dict is
   reached solely as a `/Resources`→`/XObject` value. Separately, pass 2 keys on `id(node)`, which
   contradicts the module's own stated discipline that identity comparison degrades silently on a
   parser cache miss; it should key on `idnum` like pass 1.

   **It compounds with the phase-1 lever.** That lever replaced the whole-file raw scan with a
   scan-outside-stream-payloads pass — which was the last backstop under a bad exemption. A2 with
   an *uncompressed* content stream is therefore a **both-phases miss**; the round-1 whole-file scan
   would have caught it. The skip logic itself is sound in isolation (the reviewer could not
   construct a miss through it alone, and checked `/Length` under-declaration and the
   `MAX_PDF_OBJECTS` cap), but it must not land before the exemption is fixed.

   Also verified while there: the two rewritten tests lost no coverage beyond one disclosed contract
   change — exactly one test removed, thirteen added, no skip/xfail. The dropped property
   (rejection before nonce reservation for plaintext inline images) is genuinely gone from the
   product, not just the tests, and the code discloses the cost.
2. **R3 Task 4, fix round 1** — commit `6103483` landed with its negative control confirmed
   failing-as-expected, but no scoped re-review has run. Generate the package over
   `dbe40eb..6103483` and dispatch.
3. **R2 Task 4** — never started. Brief already extracted at
   `.superpowers/sdd/2026-07-29-r2-odoo-leases-concurrency/task-4-brief.md`.

---

## 2. Environment facts that cost time to learn

- **The Odoo test command.** `docker compose exec odoo` is the **Odoo 18** container and mounts
  `odoo/addons18` — the wrong addon entirely. The v19 addon is behind the `odoo19-trial` profile:
  ```bash
  docker compose --profile odoo19-trial run --rm --no-deps -T odoo19-trial \
    odoo --no-http --test-enable --stop-after-init \
    --workers=0 --max-cron-threads=0 \
    -d masterfischer_o19_foundation_test -u picking_assistant_integration
  ```
  Every flag is load-bearing. `--max-cron-threads=0` and `run --rm` exist because the test database
  sits on the shared Postgres: a concurrently running Odoo service with cron threads writes
  `ir_cron` during module load and kills the run with `SerializationFailure` before a single test
  executes. **Never leave a long-running `odoo19-trial` container up.**
- **`masterfischer` is the LIVE database.** Tests use `masterfischer_o19_foundation_test`.
- **pytest is vendored gitignored at `backend/.deps` in the MAIN tree and does not exist in
  worktrees.** From a worktree, point `PYTHONPATH` at the main tree's copy. Do not build a venv.
  Infrastructure tests additionally need `infrastructure/scripts` on the path.
- **This is WSL2 over `/mnt/c` and it is slow.** A subagent that runs a long command without
  printing gets killed by a stall watchdog at 600 seconds. Tell agents to print between steps and
  to write their report incrementally — three agents lost work to this.
- **`backend/app/main.py` is entirely CRLF.** `git diff --check` warns on every added line; that is
  expected noise.

---

## 3. Decisions still owed by the project owner

1. **The Odoo-18 port (`odoo/addons18/`)** — decision §3.4 of the program status. It serves the
   LIVE stack and now diverges from the fixed v19 addon on at least the expired-lease callback and
   the message oracle. Either it becomes a formally approved narrow auth-compatibility port with
   its own tests and a parity matrix, or it is deleted. Until this is settled, the running system
   does not receive these fixes.
2. **The Odoo-19 cutover** — Tasks 12 and 15 stay hard-blocked until branch `codex/odoo19-cutover`
   exists and a cutover plan is written, which is what allows the `wave1-odoo19-handoff` tag. Task
   16 is blocked behind both.

3. **Possibly: whether this validator's strictness matches where artifacts really come from.** Not
   a blocker, but a data point the reviewer surfaced by accident. The one real PDF in the repo root
   (`Eigenständigkeitserklärung Stand 11-2024.pdf`) is **rejected** by the validator with "PDF must
   contain exactly one revision: malformed or polyglot" — because ordinary office software saves
   incremental updates. Pre-existing and unrelated to any lane's work. It only matters if artifacts
   are ever expected to arrive from anywhere but the generator; if they are, that assumption needs
   revisiting rather than the validator being loosened blindly.

### On the PDF false-positive rate — a correction to what I said at session end

I told the project owner this was solved and would not come back. That was overstated. The
2.1e-5 residual holds only for a document whose sole bulk binary is an *exempt raster*. Non-executed
binary streams that are not in an `/XObject` slot are still fully scanned: the reviewer confirmed a
legitimate raster is rejected when its `/SMask` or its `/ICCBased` colour-space stream happens to
carry the byte pattern, and embedded font files (`/FontFile2`) are in the same class. **For a
typical delivery note with an embedded font the residual is back at roughly 0.6 % per 200 KB.**

It is still not an owner decision and still fail-closed in the correct direction — but the number in
the report understates real documents, and there is a cheap improvement that collapses it: require
an `ID` token within a bounded distance after `BI`, since every real inline image is `BI … ID … EI`.
That same change would also narrow the disclosed show-string false positive.

---

## 4. What this session actually established

Seven remediation tasks completed across three lanes, each through implement → adversarial review →
fix round → scoped re-review. Every single task needed at least one fix round. The reviews found
things the implementers' green suites did not, and several findings were defects in **the plans**,
not the code.

Patterns worth carrying into tomorrow's work:

- **Make the guard fail before believing it.** Three separate guards in this session passed while
  proving nothing: a CSRF/dependency guard that covered 4 of 5 routes while asserting `4 == 4`; a
  fixture test that asserted "exactly one mutation" but only checked top-level keys; an allowlist
  derivation test that accepted any type appearing anywhere in the reference graph. Each was caught
  only by deliberately breaking the thing it guarded and confirming a red.
- **A race test that cannot distinguish "no deadlock" from "no race" is not a race test.** Demanding
  a positive control on R2's race tests cost one fix round and immediately surfaced a real
  pre-existing bug — a raw `psycopg2.UniqueViolation` leaking to callers because Odoo runs
  REPEATABLE READ and the collision handler re-SELECTed under a stale snapshot. Structurally
  invisible to any single-cursor test.
- **Audit every site, not the ones you changed.** R2's full lock-site audit found a *second*
  inverted path nobody had looked at.
- **Derive from the specification, not from the instruction.** The PDF whitespace class was written
  from Python's `\s` instead of ISO 32000-1 Table 1 (NUL missing → bypass). The fix I wrote then
  handed over a literal delimiter class, and the same root cause repeated (`{`/`}` → bypass). Both
  tables are now named constants substituted into the pattern so they cannot drift.
- **A test that passes against the vulnerable implementation is worth deleting.** R3's implementer
  threw away its own TOCTOU test on discovering it passed against a deliberately reverted, broken
  version, and said so.

---

## 5. Restart recipe

```bash
cd "/mnt/c/Users/endri/Desktop/Bachelor"
git worktree list                 # three trees expected
cat .superpowers/sdd/2026-07-29-r1-backend-security-boundaries/progress.md   # and r2, r3, r4
```

Then, in order: re-run R1 Task 3's fix-round-2 re-review, dispatch R3 Task 4's scoped re-review,
and restart R2 Task 4 from its existing brief. The SDD skill's scripts live at
`~/.claude/plugins/cache/claude-plugins-official/superpowers/6.2.0/skills/subagent-driven-development/scripts/`
(`sdd-workspace`, `task-brief`, `review-package`).

Docker Desktop and the `odoo19-trial` container were left stopped.
