# Environment
.env
*.env.local

# Zertifikate
infrastructure/certs/*.pem
infrastructure/certs/*.crt
infrastructure/certs/*.key

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
backend/.deps/
.pytest_cache/

# Node
node_modules/

# Playwright / browser artifacts
.playwright-cli/
.playwright-mcp/
playwright-report/
test-results/

# Generated local outputs
output/
evaluation/
/*.png
/*.zip
/Detail
/Picker
/img

# Docker
docker-compose.override.yml

# IDE
.vscode/
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Odoo
odoo/filestore/

# n8n
.n8n/
n8n/backups/
n8n/backups/**/.env.snapshot
n8n/tmp/

# Generated load-test and local priority artifacts
infrastructure/scripts/load-test-results.json
picking.priority

# Task 17 live-gate evidence. Host-owned, mode 0700, holds seed metadata with
# real database ids -- and it is written by the orchestrator on the HOST, never
# by a container, so the gate does not depend on the Odoo image's UID.
/.artifacts/

# Graphify cache
graphify-out/

# Pre-cutover database/filestore backups (contain credential hashes — never commit)
infrastructure/backups/
# File-based Docker secrets (Task 5)
infrastructure/secrets/*
!infrastructure/secrets/README.md

# Importlaeufe legen hier Zeitstempel-Verzeichnisse mit Live-Zustand ab.
n8n/backups/

# ── Agent session artifacts (2026-08-17 cleanup) ─────────────────────────────
.claude/
.serena/
.design/
docs/superpowers/
docs/parallel-chats/
docs/SESSION_*.md
CLAUDE.md
AGENTS.md
infrastructure/bildkorpus/messwerte/**/*.log

# Lokale .env-Sicherungen (enthalten Klartext-Secrets)
.env.bak-*

# Lokale mkcert-CA, nur zum Ausliefern an Testgeraete im WLAN (kein Schluesselmaterial)
pwa/rootCA.pem
pwa/rootCA.crt

# Lokale DB-Sicherungen vor Aufraeumlaeufen (enthalten Zugangs-Hashes)
backups/
